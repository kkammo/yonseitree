<p id="notice"><%= notice %></p>

<p>
  <strong>Project name:</strong>
  <%= @project.project_name %>
</p>

<p>
  <strong>Description:</strong>
  <div class="well">
  	<%= @project.description %>
	</div>
</p>

<%= render html: @content.html_safe %>

<%= link_to 'Edit', edit_directory_homework_project_path(@directory_homework, @project) %> |
<%= link_to 'Back', directory_homework_projects_path(@directory_homework) %>
