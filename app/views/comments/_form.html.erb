<%= form_for([@project, @project.comments.build]) do |f| %>

  <div class="field">
    <%= f.label "댓글" %><br />
    <%= f.text_area :body  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
